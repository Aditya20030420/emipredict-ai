# Data (large / not for repo)
data/raw/
data/processed/
*.csv

# MLflow tracking store
mlruns/
mlartifacts/
mlflow.db

# Models (may be large; keep only small selected models if committed deliberately)
models/*.pkl
models/*.joblib
!models/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Reports (generated artifacts; keep markdown, ignore heavy images if desired)
reports/*.png

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Secrets
.env
.streamlit/secrets.toml
