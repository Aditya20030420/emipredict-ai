# Full dev/training environment (superset of requirements.txt).
# Use locally for training, EDA, and MLflow; NOT needed by the deployed app.
-r requirements.txt
mlflow==3.15.2
matplotlib==3.10.8
seaborn==0.13.2
