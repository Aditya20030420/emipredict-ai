# Streamlit Cloud runtime deps (app only). Pinned to the versions the
# committed champion pickles were trained/serialized with — do NOT downgrade
# scikit-learn/xgboost or model loading will break.
# Training-only deps (mlflow, etc.) live in requirements-dev.txt.
streamlit==1.53.1
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.8.0
xgboost==3.2.0
joblib==1.5.3
cloudpickle==3.1.2
